html
    head
        title Sync - Homepage
        meta(charset="utf-8")
        meta(name="viewport" content="width=device=width, initial-scale=1.0")
        link(rel="stylesheet" href="./style/materialize.css")
        link(rel="stylesheet" href="./style/home.css")
        link(rel="manifest" href="./manifest.json")
        link(rel="icon" href="./img/logo.png" type="image")
        script(src="./js/materialize.js")
        script(src="./js/jquery.js")
        script(src="./js/home.js")
    body
        header
            div#logo
                img#logoImg(src="./img/logo.png")
                p Sync
            div
                img#login(src="./img/icons/login.png")
                img#logout(src="./img/icons/logout.png")
        div#content
            //Image instead of banner at some point
            div#banner
                div#createRoomCard.card.white
                    div.card-content.black-text
                        span.card-title Welcome to Sync
                        p Looking to watch some videos together with friends? This is the right place for you! Create a room to start.
                    div.card-action
                        a(onclick='createRoom()') Create room
            
        footer
            hr
            p Contact me on ricardojg1@hotmail.com